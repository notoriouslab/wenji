diff a/src/wenji/search/rewrite.py b/src/wenji/search/rewrite.py	(rejected hunks)
@@ -14,6 +14,8 @@ from datetime import datetime, timedelta, timezone
 
 import httpx
 
+from wenji.core.safety import sanitize_prompt_input
+
 logger = logging.getLogger("wenji.search.rewrite")
 
 DEFAULT_TIMEOUT_S = 1.5
@@ -30,7 +32,7 @@ REWRITE_PROMPT_TEMPLATE = (
     "範例：\n"
     "因信稱義 → 因信稱義 教義 | 路德 改教 因信稱義 | 保羅 羅馬書 信心\n"
     "禱告怎麼禱告 → 禱告 方法 步驟 | 主禱文 教導 | 禱告 蒙應允 條件\n\n"
-    "使用者查詢：{query}"
+    "使用者查詢：<query>{query}</query>"
 )
 
 
@@ -75,7 +77,7 @@ class QueryRewriter:
         self.conn.commit()
 
     def _call_api(self, raw: str) -> str:
-        prompt = self.prompt_template.format(query=raw)
+        prompt = self.prompt_template.format(query=sanitize_prompt_input(raw))
         response = httpx.post(
             self.api_url,
             headers={"Authorization": f"Bearer {self.api_key}"},
