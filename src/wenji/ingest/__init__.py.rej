diff a/src/wenji/ingest/__init__.py b/src/wenji/ingest/__init__.py	(rejected hunks)
@@ -431,7 +431,8 @@ def rebuild_from_disk(
     """
     from wenji.core.db import initialise_schema
 
-    for tbl in ("articles_meta", "articles_fts", "chunks_fts", "doc_vectors", "article_axes"):
+    known_tables = {"articles_meta", "articles_fts", "chunks_fts", "doc_vectors", "article_axes"}
+    for tbl in known_tables:
         conn.execute(f"DELETE FROM {tbl}")
     conn.execute(
         "UPDATE wenji_meta SET value = '0' WHERE key IN ('n_articles', 'n_chunks', 'n_doc_vectors')"
